name: HA Framework KontrolDom
slug: ha_framework_kontroldom
description: "Automatyczny installer frameworku KontrolDom dla Home Assistant"
version: "0.1.0"
stage: experimental
url: "https://github.com/kontroldom/ha-framework"
arch:
  - amd64
  - aarch64
  - armv7
init: false
startup: services
boot: manual
homeassistant_api: true
supervisor_api: true

map:
  - config:rw
  - share:rw

options:
  dry_run: false
  overwrite_existing: false
schema:
  dry_run: bool
  overwrite_existing: bool
